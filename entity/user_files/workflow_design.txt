Sure! Let's outline the workflow functions focused on the business logic for the TeamCity integration based on your previous requirements.

### Workflow Functions (Processors)

#### Entity: Deployment Configuration

1. **Function: CreateDeploymentConfiguration**
   - **Input**: `user_name`
   - **Business Logic**:
     ```pseudocode
     FUNCTION CreateDeploymentConfiguration(user_name):
         // Validate the input
         IF user_name IS NULL OR user_name IS EMPTY:
             RETURN ERROR "Invalid user name"
         
         // Prepare the API request payload
         payload = {
             "buildType": {
                 "id": "KubernetesPipeline_CyodaSaas"
             },
             "properties": {
                 "property": [
                     {
                         "name": "user_defined_keyspace",
                         "value": user_name
                     },
                     {
                         "name": "user_defined_namespace",
                         "value": user_name
                     }
                 ]
             }
         }
         
         // Call TeamCity API to initiate the deployment job
         build_id = TeamCityAPI.POST("/app/rest/buildQueue", payload)
         
         // Return the build ID for tracking
         RETURN build_id
     ```

#### Entity: User Application Deployment

2. **Function: DeployUserApp**
   - **Input**: `repository_url`, `is_public`, `user_name`
   - **Business Logic**:
     ```pseudocode
     FUNCTION DeployUserApp(repository_url, is_public, user_name):
         // Validate inputs
         IF repository_url IS NULL OR repository_url IS EMPTY:
             RETURN ERROR "Invalid repository URL"
         
         // Prepare the API request payload for user application deployment
         payload = {
             "buildType": {
                 "id": "KubernetesPipeline_CyodaSaasUserEnv"
             },
             "properties": {
                 "property": [
                     {
                         "name": "user_defined_keyspace",
                         "value": user_name
                     },
                     {
                         "name": "user_defined_namespace",
                         "value": user_name
                     }
                 ]
             }
         }
         
         // Call TeamCity API to initiate the user application deployment job
         user_build_id = TeamCityAPI.POST("/app/rest/buildQueue", payload)
         
         // Return the user build ID for tracking
         RETURN user_build_id
     ```

#### Entity: Status and Statistics

3. **Function: GetDeploymentStatus**
   - **Input**: `build_id`
   - **Business Logic**:
     ```pseudocode
     FUNCTION GetDeploymentStatus(build_id):
         // Validate the build ID
         IF build_id IS NULL OR build_id IS EMPTY:
             RETURN ERROR "Invalid build ID"
         
         // Call TeamCity API to fetch the deployment status
         deployment_status = TeamCityAPI.GET("/app/rest/buildQueue/id:" + build_id)
         
         // Return the deployment status
         RETURN deployment_status
     ```

4. **Function: GetDeploymentStatistics**
   - **Input**: `build_id`
   - **Business Logic**:
     ```pseudocode
     FUNCTION GetDeploymentStatistics(build_id):
         // Validate the build ID
         IF build_id IS NULL OR build_id IS EMPTY:
             RETURN ERROR "Invalid build ID"
         
         // Call TeamCity API to fetch the deployment statistics
         deployment_statistics = TeamCityAPI.GET("/app/rest/builds/id:" + build_id + "/statistics/")
         
         // Return the deployment statistics
         RETURN deployment_statistics
     ```

5. **Function: CancelUserAppDeployment**
   - **Input**: `build_id`
   - **Business Logic**:
     ```pseudocode
     FUNCTION CancelUserAppDeployment(build_id):
         // Validate the build ID
         IF build_id IS NULL OR build_id IS EMPTY:
             RETURN ERROR "Invalid build ID"
         
         // Prepare the payload for cancellation
         payload = {
             "comment": "Canceling a queued build",
             "readdIntoQueue": false
         }
         
         // Call TeamCity API to cancel the deployment job
         TeamCityAPI.POST("/app/rest/builds/id:" + build_id, payload)

         // Return success message
         RETURN "Build with ID " + build_id + " has been canceled successfully"
     ```

### Summary
Each of these workflow functions (processors) encapsulates the relevant business logic related to TeamCity integration for deployment configurations and user applications. The working of the respective function is aimed at providing a clear interaction with the TeamCity API while ensuring that valid input is taken into account to prevent errors. Each function is focused on one entity's operation, adhering to a clear separation of concerns.